// I should read the file first to preserve content, but I don't want to rewrite the whole file blindly.
// Better to use SearchReplace or just use the tool to revert the change if possible.
// Wait, I appended using `Add-Content`. I should remove the last line.
